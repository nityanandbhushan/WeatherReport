<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Content</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Conten here</h1> 
    <hr>
    <%for(item of posts){ %>
        <div>
            <p>Id:<%=item.id%></p>
            <p>@<%=item.username%></p>
            <p><%=item.content%></p>
            <br> 
            <div>
                <a href="http://localhost:3000/show/<%=item.id%>">Show in details</a> 
                <a href="http://localhost:3000/show/<%=item.id%>/edit" id="edit">Edit Post</a>
            </div>
           
        </div>
        <hr>
        <% }%> 
        <a href="http://localhost:3000/show/new">Create new post</a>
</body>
</html>